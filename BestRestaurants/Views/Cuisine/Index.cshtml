@using BestRestaurants.Models;

<h1> Cuisines </h1>

@if(@Model.Count == 0)
{
  <h3> No Cuisines Available</h3>
}

<ul>
  @foreach(Cuisine cuisine in Model)
  {
    <li>@Html.ActionLink($"{cuisine.Name}", "Details", new { id = cuisine.CuisineId })</li>
  }
</ul>

<p>@Html.ActionLink("Create new cuisine", "Create")</p>